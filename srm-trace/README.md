# SRM Trace

Run:
1. Install JDK 17
2. Install Maven
3. Open folder in VS Code
4. Run `mvn clean javafx:run`

Default admin: username=admin password=admin

DB file at `data/srmtrace.db`.
Images/QR codes saved in `data/images` and `data/qrcodes`.
